# Shared Files

This repository is used for public file sharing.

Files are staged under `files/YYYYMMDD/`.

If a zip filename contains a 5-digit code and `PasswordProtected` is true in the share output, use the password printed by the packaging script.
